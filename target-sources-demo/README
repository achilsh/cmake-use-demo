```
    1. 向目标（可执行文件或者库）添加依赖的源文件
    2. 作用：用于向已经创建的目标（如可执行文件或库）添加源文件。
    它允许在不同的位置添加源文件，而不是仅在 add_executable() 或 add_library() 时一次性添加所有源文件，提供了更灵活的源文件管理方式。

    3. 使用场景
        3.1 逐步添加源文件
        3.2 在子目录中添加源文件
        
``` 